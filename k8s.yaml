# this is only an EXAMPLE of deploying casddor in kubernetes
# please modify this file according to your requirements
apiVersion: v1
kind: Service
metadata:
  #EDIT IT: if you don't want to run iam in default namespace, please modify this field
  #namespace: iam
  name: iam-svc
  labels:
    app: iam
spec:
  #EDIT IT: if you don't want to run iam in default namespace, please modify this filed
  type: NodePort
  ports:
    - port: 8000
  selector:
    app: iam
---
apiVersion: apps/v1
kind: Deployment
metadata:
  #EDIT IT: if you don't want to run iam in default namespace, please modify this field
  #namespace: iam
  name: iam-deployment
  labels:
    app: iam
spec:
  #EDIT IT: if you don't use redis, iam should not have multiple replicas
  replicas: 1
  selector:
    matchLabels:
      app: iam
  template:
    metadata:
      labels:
        app: iam
    spec:
      containers:
        - name: iam-container
          image: hanzoai/iam:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          volumeMounts:
            # the mounted directory path in THE CONTAINER
            - mountPath: /conf
              name: conf
          env:       
            - name: RUNNING_IN_DOCKER
              value: "true"
      #if you want to deploy this in real prod env, consider the config map
      volumes:
        - name: conf
          hostPath:
            #EDIT IT: the mounted directory path in THE HOST
            path: /conf
