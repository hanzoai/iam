# Neutralized — build/deploy now runs natively on Hanzo CI (act_runner in-cluster).
# The ONE canonical pipeline is .hanzo/workflows/deploy.yml: Hanzo Git push →
# BuildKit builds Dockerfile → ghcr.io/hanzoai/iam:<sha> → kubectl patch app iam
# → operator reconcile. GitHub is a downstream mirror; it no longer builds images.
name: build
on:
  workflow_dispatch:

jobs:
  sync-notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "native pipeline is .hanzo/workflows/deploy.yml; GitHub is a mirror"
